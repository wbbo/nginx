server {
    listen 13.168.1.11:20000;# 13.168.1.11监听地址， 20000监听端口
    proxy_connect_timeout 30s; #服务侧超时时间
    proxy_timeout  30s;#客户端侧超时时间
    proxy_pass 2.2.2.2:20000;# 2.2.2.2为目的对象： 20000为目的端口
    alg_proto UDP_NULL;  #此处为后台传递的协议类型变量
    alg_port_min 0;
    alg_port_max 0;
}

